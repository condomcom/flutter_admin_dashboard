import 'package:flutter/material.dart';

class ConferenceEditScreen extends StatelessWidget {
  const ConferenceEditScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
